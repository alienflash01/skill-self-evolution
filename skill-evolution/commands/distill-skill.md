---
description: Manually trigger task-level skill distillation from the current session
allowed-tools: Bash, Read
---

# /distill-skill — Task-level Skill Distillation (Layer 2)

Delegate to the `skill-distiller` subagent to capture reusable techniques from
the current work session into `~/.claude/skills/`.
